console.log('Test');

// Declare foo only once
let foo = "bar";  // This can be reassigned if needed
console.log(foo);

// If you need to assign a new value, just reassign it instead of redeclaring
foo = 42;  // Reassign the value to the existing foo
console.log(foo);
