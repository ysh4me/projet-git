name: Linter

on:
  push:
    branches:
      
main
develop
pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      
name: Checkout code
      uses: actions/checkout@v3

      
name: Lancer le linter
      uses: wzieba/eslint-action@v2
      with:
        eslintConfig: '.eslintrc.json'
        lintFilePatterns: '*/.js' 