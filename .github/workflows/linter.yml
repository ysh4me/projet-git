name: Linter
on:
  push:
    branches:
      - main
      - develop
  pull_request:
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Installer les dépendances
        run: npm install
      - name: Lancer le linter
        run: npx eslint .