#!/bin/bash

# Exécuter le linter (ajuste cette commande selon le linter utilisé)
echo "Running linter..."
eslint . || exit 1  # Si le linter échoue, empêcher le commit

echo "Linter passed, proceeding with commit..."
echo "HOOK ACTIVÉ !" >> hook_debug.log
